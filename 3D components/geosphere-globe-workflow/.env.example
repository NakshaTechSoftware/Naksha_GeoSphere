# Optional: production raster/vector style URL for the local map stage.
# Leave empty to use the built-in legally usable demo style (light pale-blue canvas).
# NOTE: demo tiles are NOT the final commercial basemap.
VITE_MAP_STYLE_URL=

# Optional: access token for a provider that requires one (e.g. MapTiler).
VITE_MAP_ACCESS_TOKEN=
