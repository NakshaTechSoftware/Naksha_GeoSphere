# Module: application — placeholder.
#
# No cloud resources are defined here yet. See README.md for the planned
# compute (web/api/worker) and load balancer this module will contain
# once a target cloud provider is chosen. Container images referenced
# here must always be pinned (never ":latest"), consistent with the rest
# of this repository.

locals {
  not_implemented = "application module not yet implemented — see infrastructure/terraform/modules/application/README.md"
}
