# Module: object-storage — placeholder.
#
# No cloud resources are defined here yet. See README.md for the planned
# private S3-compatible buckets this module will contain once a target
# cloud provider is chosen. All four buckets must remain private — no
# public bucket policy should ever be introduced here (see
# docs/SECURITY.md).

locals {
  not_implemented = "object-storage module not yet implemented — see infrastructure/terraform/modules/object-storage/README.md"
}
