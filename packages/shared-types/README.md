# @naksha/shared-types

Shared TypeScript types for Naksha GeoSphere. Currently holds only the
`ServiceHealthStatus` type used by the platform health check. Future
marketplace domain types (dataset, AOI, order, license, download) will be
added here as those modules are built, so the frontend and any future
TypeScript clients share one source of truth instead of duplicating types.
