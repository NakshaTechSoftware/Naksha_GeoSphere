/**
 * Naksha GeoSphere brand tokens, shared as plain values so they can be
 * consumed outside CSS (e.g. by future native clients, email templates,
 * or design tooling). The canonical CSS-variable form used by
 * `apps/web` lives in `apps/web/src/styles/tokens.css` — keep both in
 * sync if either changes.
 */
export const brandColors = {
  spatialNavy: "#0B1F33",
  geoTeal: "#0F8B8D",
  cloudMist: "#F3F7F9",
} as const;
